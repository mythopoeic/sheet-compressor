# Fixtures

Language-neutral golden-fixture corpus: input grids (+ chart descriptors) and their expected compressed outputs. Generated by the TypeScript reference implementation; every language diffs against these.
