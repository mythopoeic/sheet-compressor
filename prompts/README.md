# Prompts

Shared prompt templates: per-encoding reader explainers + task templates (table/region detection, cell lookup, sheet Q&A) + how to read CHART(...) descriptors. Authored once here, mirrored into each package.
